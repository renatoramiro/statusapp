<% if @site.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@site.errors.count, "error") %> prohibited this site from being saved:</h2>

    <ul>
    <% @site.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<label>Nome</label>
<%= f.text_field :name, class: 'span5', placeholder: "Ex.: Site de compras" %>

<label>Url</label>
<%= f.text_field :url, class: "span4", placeholder: "Ex.: http://www.example.com" %>

<label class="checkbox" >
	<%= f.check_box :public %> Tornar esse site público?
</label>

<%= f.hidden_field :user_id %>

